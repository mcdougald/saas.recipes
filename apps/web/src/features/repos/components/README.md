# Repository Components

This folder groups dashboard presentation by feature directory:

- `repository-list/` - dashboard collection view with list cards and trend visuals.
- `repository-detail/` - `/dashboard/[slug]` tabbed detail view.

Each directory has its own README describing the data surfaced and file-level responsibilities.
